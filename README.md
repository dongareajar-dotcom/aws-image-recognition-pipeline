# aws-image-recognition-pipeline
This project is a fully serverless image recognition system built using AWS services. Whenever an image is uploaded to an S3 bucket, it automatically triggers a Lambda function that uses Amazon Rekognition to detect objects in the image and stores the results in DynamoDB.
